# Манифест требований

Источник: `2026-09-02-brief.md` (фидбек владельца). Строку может снять только пользователь.

| ID | Из брифа (дословно) | Статус | Основание | Где |
|----|---------------------|--------|-----------|-----|
| R01 | «мне не нравится то что не виден полностью хиро блок при открытии сайта» — hero должен помещаться в первый экран без прокрутки | done | брифинг: «Весь hero, включая полосу доверия». `.hero-content` padding уплотнён (top→`clamp(100,13vh,150)`, bottom→`clamp(88,13vh,132)`), `.hero-ctas`/`.hero-meta` margin-top 38/44→30/32; `vh`-единицы сохранены; фото и `<h1>` не тронуты. Вживую не отрендерено — расчёт высот + инспекция HTML | globals.css · **глаз владельца** |
| R02 | «убери навигация слева, она потеряла смысл» — убрать боковой рейл (`.side-rail` / `components/home/SideRail.tsx`) | done | `SideRail.tsx` удалён, импорт+usage из `app/page.tsx` убраны, блок `.side-rail*` вырезан из `globals.css`; grep=0; в HTML `/` рейла нет | globals.css, page.tsx |
| R03 | «мне не нравиться то как выглядят ссылки, может стоит убрать подчеркивание» — убрать подчёркивание у акцентных текстовых ссылок | done | брифинг: «У всех акцентных ссылок; сигнал — цвет + стрелка →». `border-bottom`/`borderBottom` снят во всех 6 точках; цвет и `→` сохранены; hover=осветление, focus=`outline` (a11y-плюс); инлайн-ссылки — через `a[style*="--accent"]`, без нового класса | globals.css + 4 компонента |
| R04 | «номер телефона маловат» — увеличить номер телефона в хедере (`.call-pill`) | done | `.call-pill` font-size 14→15px (в диапазоне спеки 15–16); на ≤480px номер больше не прячется. На нижней границе «заметно крупнее» — **вынесено на глаз владельцу** | globals.css · **глаз владельца** |
| R05 | «мне не очень нравится эта точка» — убрать/переосмыслить зелёную точку-индикатор в `.call-pill` (`.call-dot`) | done | `<span className="call-dot" />` и `.call-dot` удалены, `gap:9px` из `.call-pill` убран. Побочно: на ≤480px вернул видимость номера (пилюля становилась пустой) — чуть шире буквы R05 | globals.css, Header.tsx |
| R06 | «почему в хэдере я не могу просто навестись на нужное меню а потом выбрать из дропдауна, а мне обязательно нужно сначала нажать» — дропдауны хедера открываются по наведению на десктопе, выбор пункта работает | done | Мост `.nav-dropdown::before` перекрыл зазор триггер↔панель; `toggleGroup` — no-op на ≥861px (санкция спеки), клик-тап сохранён для <861px; `:focus-within` работает. Вживую не отрендерено | globals.css, Header.tsx · **глаз владельца** |
| R07i | *(рамка)* не ломать существующий функционал: форма, JSON-LD, sitemap, редиректы, SSG, тесты (29 зелёных, `tsc` 0) | done | `npm test` 29 · `tsc` 0 · `build` зелёный (28 SSG + `/api/book`) · редиректы 7/7 → 308 (`next.config.ts` не тронут) · JSON-LD/форма/sitemap в HTML без изменений | верификация оркестратора |
| R08i | *(рамка)* не трогать: контент `data/*`, `data/business.siteUrl`, `AggregateRating` | done | ни один файл `data/*` в диффе; `siteUrl`/`AggregateRating` не тронуты | дифф |
| R09i | *(рамка)* оформить пересмотр принципа «не трогать дизайн» как приписки к ADR (в первую очередь `docs/adr/0002`) | done | приписка к `docs/adr/0002` (датирована 2026-09-03, коммит `6f2ae17`): принцип «заморожен / правку → BLOCKED» снят решением владельца, перечень 6 отступлений, что осталось в силе. Отдельный ADR 0014 не заводился. CLAUDE.md обновлён между маркерами | `docs/adr/0002`, CLAUDE.md |
| R10i | *(рамка)* вне периметра: всё, что не про эти 5 пунктов — не начинать | done | дифф строго в периметре 6 правок | дифф |
| R11i | *(рамка)* короткий отчёт: что изменено, какие ADR пересмотрены и почему, что решено самостоятельно | done | отчёт выдан 2026-09-03 | чат |

## Требует уточнения в брифинге — закрыто

- **R01** ✅ уточнено 2026-09-02 (AskUserQuestion): «Весь hero, включая полосу доверия».
  В первый экран на десктопе должны помещаться h1 + lede + кнопки + акцентная ссылка +
  `.hero-meta` (рейтинг/часы) + `.hero-trust` (Same-Day / Warranty / $75 Diagnostic).
  Фото и `<h1>` не трогаем.
- **R03** ✅ уточнено 2026-09-02 (AskUserQuestion): «У всех акцентных ссылок; сигнал — цвет +
  стрелка →». Подчёркивание/`border-bottom` убрать у ВСЕХ акцентных текстовых ссылок сайта
  (hero-переключатель, «See … →» в `#who-we-serve` и `#brands`, `.brand-note a`,
  `.not-listed a`, ссылки на страницах техники). Сигнал «это ссылка» = лаймовый цвет +
  стрелка `→` там, где она уже есть. Ховер — лёгкий сдвиг стрелки / изменение яркости.

## Гейт G2 (после спецификации) — ПРОЙДЕН · 2026-09-02

Свой проход: все 11 строк `in-spec`, ноль `open`. Независимая проверка: субагент
получил только `2026-09-02-brief.md` + `spec.md`. Находки (2 «не покрыто», 2 «наполовину»,
8 «есть в спеке, нет в брифе») разобраны — детали в `state.js` → `coverage`. Спека и
`brief.md` («Дополнения») дополнены; расхождений, требующих новой работы, не осталось.

## Гейт G3 (после плана) — н/п · ярус T0

Тасков нет (ярус T0, `spec.md` → «Границы и швы»). Все R01–R11i уходят в один проход
сборки одним субагентом по `spec.md` + `interfaces.md`. Forward/backward-трассировка —
через таблицу «Покрытие манифеста» в `spec.md`.

## Гейт G1 (после брифинга) — ПРОЙДЕН · 2026-09-02

Все 11 строк имеют статус. Открытых вопросов не осталось: обе развилки (R01 — что
помещается в первый экран; R03 — область и замена сигнала) закрыты ответами владельца
через AskUserQuestion. `Статус: open` у всех строк = «ещё не разложено по спецификации»,
это норма для этой фазы; в спецификацию идут все R01–R06, рамки R07i–R11i переходят как
ограничения. Ничего не снято и не отложено.
