# 04 — Финальная проверка (ПРОВЕРКА ПЕРЕД СДАЧЕЙ)

**Требования:** R26, R27, R28 (e2e-часть), R29 (проверка), R30
**Blocked by:** 01, 02, 03
**Зона:** верификация (без правок кода, кроме точечных фиксов найденных регрессий) · финальный коммит
**Волна:** 2
**Status:** ready

## Что должно заработать

Всё из «ПРОВЕРКА ПЕРЕД СДАЧЕЙ» брифа выполнено и зелёное.

## Из брифа, дословно

> «npm run build, npm run typecheck и npm test проходят без ошибок.»
> «Прогон по страницам (…) на 1440px и 390px: нет текста с контрастом ниже 4.5:1,
> нет горизонтального скролла, нет ошибок в консоли, все изображения загружаются.»
> «Сквозной тест формы: некорректная заявка → 400 с полем-ошибкой, корректная → 200
> и лид уходит во все включённые каналы (проверить с мок-вебхуком).»
> «Старые .html URL отдают постоянный редирект на чистые роуты.»
> «Проверить, что в рабочем дереве уже стоят намеренные удаления трёх устаревших
> брифов … — закоммить их вместе со своими изменениями.»

## Разделы спецификации

Истории 26–29; Решения §Тесты.

## Критерии приёмки

- [ ] `npm run build` · `npm run typecheck` (`tsc --noEmit`) · `npm test` — все три без ошибок
- [ ] `npm run build && npm start`, Playwright (или аналог): скриншоты `/`, `/for-business`,
      `/about`, `/brands`, `/towns`, `/towns/charlotte`, `/appliance-repair/refrigerator`
      на 1440×900 и 390×844
- [ ] программный обход DOM: для каждого текстового узла — коэффициент контраста к
      фактическому фону; **≥ 4.5:1 для текста, за который отвечает этот заход**
      (заголовки `#who-we-serve` — главная цель). Унаследованный низкий контраст,
      который бриф трогать не велит (issues §2.11 — акцентная подпись бокового рейла
      на светлых секциях) — фиксируется строкой отчёта, не провал
- [ ] нет горизонтального переполнения ни на одной из 7 страниц на обоих размерах
- [ ] перехват ошибок консоли и неудачных запросов — пусто; все `<img>`/`next/image`
      грузятся (naturalWidth > 0)
- [ ] `curl -I` по всем 7 старым `.html` → `308` + `location` на чистый роут
- [ ] форма: `POST /api/book` невалидным телом → 400 + поле-ошибка; валидным → 200;
      с `BOOK_WEBHOOK_URL` на локальный мок-приёмник → мок получил JSON лида
- [ ] нет кода под то, что вне периметра (hero-анимация, онлайн-запись, блог, E-E-A-T,
      города сверх 5, офлайн-шаги)
- [ ] `data/business.siteUrl` не изменён; `AggregateRating` в `lib/jsonld` не тронут
- [ ] `git status`: удаления `ek-global-website-brief.md`, `ek-global-b2b-priority-brief.md`,
      `ek-global-nextjs-master-brief.md` присутствуют — `git rm`/stage их для финального коммита
- [ ] если проверка нашла регрессию — точечный фикс в соответствующей зоне (не рефакторинг),
      иначе `BLOCKED` с описанием
- [ ] дописать `interfaces.md` раздел «### Таск 04» — итог проверки (числа, скриншоты, что нашлось)
